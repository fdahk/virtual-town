# 数据模型与接口契约 V1

> 目的：作为前后端正式编码的第一版契约。后端 Pydantic、数据库模型、OpenAPI、前端 TypeScript 类型都以本文档为准。

---

## 1. 命名规范

### 1.1 ID 命名

所有业务主键使用 UUID，接口中统一使用字符串。

示例：

```json
{
  "agent_id": "8b7f8d2e-1a9f-4c5f-9f0a-000000000001"
}
```

### 1.2 时间

所有接口时间字段使用 ISO 8601：

```text
2026-04-30T08:00:00+08:00
```

### 1.3 坐标

坐标为 tile 坐标，不是像素坐标。

```json
{
  "x": 12,
  "y": 18
}
```

前端 Phaser 内部像素坐标由 `tile_size` 换算。

---

## 2. 枚举定义

### 2.1 EntityType

```ts
type EntityType = "human" | "animal" | "player";
```

### 2.2 SceneType

```ts
type SceneType = "outdoor" | "indoor";
```

### 2.3 AgentState

```ts
type AgentState =
  | "IDLE"
  | "THINKING"
  | "PLANNING_PATH"
  | "MOVING"
  | "INTERACTING"
  | "CHATTING"
  | "WAITING"
  | "SLEEPING"
  | "BLOCKED"
  | "DROWNING"
  | "PANIC";
```

### 2.4 MemoryType

```ts
type MemoryType = "event" | "thought" | "chat" | "summary";
```

### 2.5 MemoryScope

```ts
type MemoryScope = "working" | "short_term" | "long_term";
```

### 2.6 WorldEventType

```ts
type WorldEventType =
  | "agent.moved"
  | "agent.entered_location"
  | "agent.left_location"
  | "agent.action_started"
  | "agent.action_finished"
  | "dialogue.message_created"
  | "world.object_interacted"
  | "world.hazard_triggered"
  | "animal.reacted"
  | "memory.created"
  | "llm.task_started"
  | "llm.task_finished";
```

---

## 3. 核心通用模型

### 3.1 TilePosition

```json
{
  "x": 12,
  "y": 18
}
```

TypeScript：

```ts
interface TilePosition {
  x: number;
  y: number;
}
```

### 3.2 Bounds

```ts
interface Bounds {
  x: number;
  y: number;
  width: number;
  height: number;
}
```

---

## 4. 世界模型

### 4.1 MapScene

```ts
interface MapScene {
  id: string;
  name: string;
  scene_type: SceneType;
  width: number;
  height: number;
  tile_size: number;
  tiled_map_url: string;
}
```

### 4.2 MapTile

```ts
interface MapTile {
  scene_id: string;
  x: number;
  y: number;
  terrain: string;
  walkable: boolean;
  blocks_movement: boolean;
  blocks_vision: boolean;
  hazard_type?: string | null;
  hazard_level?: number | null;
  tags: string[];
}
```

### 4.3 Location

```ts
interface Location {
  id: string;
  scene_id: string;
  name: string;
  location_type: "building" | "room" | "outdoor_area" | "facility";
  bounds: Bounds;
  entry_tiles: TilePosition[];
  open_hours?: {
    start: string;
    end: string;
  } | null;
  tags: string[];
}
```

### 4.4 Portal

```ts
interface Portal {
  id: string;
  from_scene_id: string;
  from_tile: TilePosition;
  to_scene_id: string;
  to_tile: TilePosition;
  interaction_type: "auto_enter" | "click_enter";
  requires_permission: boolean;
}
```

### 4.5 WorldObject

```ts
interface WorldObject {
  id: string;
  scene_id: string;
  name: string;
  object_type: "furniture" | "facility" | "barrier" | "plant" | "decoration" | "item";
  position: TilePosition;
  size: {
    width: number;
    height: number;
  };
  blocks_movement: boolean;
  available_interactions: string[];
  state: Record<string, unknown>;
  tags: string[];
}
```

---

## 5. Agent 模型

### 5.1 AgentProfile

```ts
interface AgentProfile {
  id: string;
  entity_type: EntityType;
  name: string;
  age?: number;
  gender?: string;
  species?: "human" | "dog" | "cat";
  appearance: {
    sprite_sheet: string;
    hair?: string;
    clothes?: string;
    color?: string;
    scale?: number;
  };
  occupation?: string;
  personality: string[];
  background: string;
  lifestyle?: string;
  long_term_goals: string[];
  home_location_id?: string | null;
}
```

### 5.2 AgentRuntimeState

```ts
interface AgentRuntimeState {
  agent_id: string;
  scene_id: string;
  position: TilePosition;
  state: AgentState;
  emotion?: string;
  energy: number;
  hunger: number;
  social_need?: number;
  fear?: number;
  status_effects: string[];
  current_action_id?: string | null;
  current_goal?: string | null;
  updated_at: string;
}
```

### 5.3 Relationship

```ts
interface Relationship {
  id: string;
  from_agent_id: string;
  to_entity_id: string;
  familiarity: number;
  trust: number;
  affection: number;
  fear: number;
  summary?: string | null;
  updated_at: string;
}
```

---

## 6. 记忆模型

### 6.1 Memory

```ts
interface Memory {
  id: string;
  agent_id: string;
  memory_type: MemoryType;
  scope: MemoryScope;
  subject?: string | null;
  predicate?: string | null;
  object?: string | null;
  description: string;
  importance: number;
  emotional_valence: number;
  keywords: string[];
  evidence_memory_ids: string[];
  created_at: string;
  last_accessed_at?: string | null;
  ttl_expires_at?: string | null;
}
```

### 6.2 MemorySearchRequest

```ts
interface MemorySearchRequest {
  query: string;
  limit?: number;
  include_short_term?: boolean;
  include_long_term?: boolean;
}
```

### 6.3 MemorySearchResult

```ts
interface MemorySearchResult {
  memory: Memory;
  score: number;
  score_detail: {
    relevance: number;
    importance: number;
    recency: number;
    relationship_relevance?: number;
  };
}
```

---

## 7. 行动与事件模型

### 7.1 AgentAction

```ts
interface AgentAction {
  id: string;
  agent_id: string;
  action_type:
    | "move_to_location"
    | "move_to_entity"
    | "interact_with_object"
    | "talk_to_entity"
    | "wait"
    | "sleep"
    | "animal_react";
  description: string;
  target_location_id?: string | null;
  target_entity_id?: string | null;
  target_object_id?: string | null;
  start_time: string;
  end_time?: string | null;
  status: "pending" | "running" | "finished" | "failed" | "cancelled";
  metadata: Record<string, unknown>;
}
```

### 7.2 WorldEvent

```ts
interface WorldEvent {
  id: string;
  simulation_id: string;
  event_type: WorldEventType;
  source: string;
  actor_entity_id?: string | null;
  target_entity_id?: string | null;
  location_id?: string | null;
  scene_id?: string | null;
  description: string;
  importance: number;
  payload: Record<string, unknown>;
  created_at: string;
}
```

---

## 8. REST API 契约

### 8.1 World API

| 方法 | 路径 | 请求 | 响应 |
|------|------|------|------|
| `GET` | `/api/world/scenes` | - | `MapScene[]` |
| `GET` | `/api/world/scenes/{scene_id}` | - | `MapScene` |
| `GET` | `/api/world/scenes/{scene_id}/tiles` | - | `MapTile[]` |
| `GET` | `/api/world/locations` | `scene_id?` | `Location[]` |
| `GET` | `/api/world/objects` | `scene_id?` | `WorldObject[]` |
| `POST` | `/api/world/pathfinding` | `PathfindingRequest` | `PathfindingResponse` |
| `POST` | `/api/world/interactions` | `WorldInteractionRequest` | `WorldInteractionResponse` |

### 8.2 Agent API

| 方法 | 路径 | 请求 | 响应 |
|------|------|------|------|
| `GET` | `/api/agents` | - | `AgentProfile[]` |
| `GET` | `/api/agents/{agent_id}` | - | `AgentProfile` |
| `GET` | `/api/agents/{agent_id}/state` | - | `AgentRuntimeState` |
| `PATCH` | `/api/agents/{agent_id}/profile` | `AgentProfilePatch` | `AgentProfile` |
| `GET` | `/api/agents/{agent_id}/relationships` | - | `Relationship[]` |
| `GET` | `/api/agents/{agent_id}/memories` | - | `Memory[]` |
| `POST` | `/api/agents/{agent_id}/memory/search` | `MemorySearchRequest` | `MemorySearchResult[]` |

### 8.3 Player API

| 方法 | 路径 | 请求 | 响应 |
|------|------|------|------|
| `POST` | `/api/players` | `CreatePlayerRequest` | `AgentProfile` |
| `GET` | `/api/players/me` | - | `AgentProfile` |
| `PATCH` | `/api/players/me/profile` | `PlayerProfilePatch` | `AgentProfile` |
| `POST` | `/api/players/me/move` | `PlayerMoveRequest` | `PlayerMoveResponse` |
| `POST` | `/api/players/me/interact` | `PlayerInteractRequest` | `PlayerInteractResponse` |
| `POST` | `/api/players/me/talk` | `PlayerTalkRequest` | `PlayerTalkResponse` |

### 8.4 Simulation API

| 方法 | 路径 | 请求 | 响应 |
|------|------|------|------|
| `POST` | `/api/simulations` | `CreateSimulationRequest` | `Simulation` |
| `GET` | `/api/simulations/{simulation_id}/state` | - | `SimulationState` |
| `POST` | `/api/simulations/{simulation_id}/start` | - | `Simulation` |
| `POST` | `/api/simulations/{simulation_id}/pause` | - | `Simulation` |
| `POST` | `/api/simulations/{simulation_id}/resume` | - | `Simulation` |
| `POST` | `/api/simulations/{simulation_id}/step` | - | `SimulationStateDelta` |
| `POST` | `/api/simulations/{simulation_id}/speed` | `SetSpeedRequest` | `Simulation` |

---

## 9. WebSocket 契约

连接地址：

```text
ws://localhost:8000/ws/simulations/{simulation_id}
```

### 9.1 通用消息

```ts
interface WebSocketMessage<TPayload> {
  version: "1.0";
  type: string;
  payload: TPayload;
  sent_at: string;
}
```

### 9.2 simulation.delta

```ts
interface SimulationDeltaPayload {
  step: number;
  world_time: string;
  entity_updates: AgentRuntimeState[];
  events: WorldEvent[];
}
```

### 9.3 dialogue.message_created

```ts
interface DialogueMessageCreatedPayload {
  conversation_id: string;
  speaker_id: string;
  target_id: string;
  text: string;
  emotion?: string;
  animation?: string;
  created_at: string;
}
```

### 9.4 world.scene_changed

```ts
interface SceneChangedPayload {
  entity_id: string;
  from_scene_id: string;
  to_scene_id: string;
  position: TilePosition;
}
```

### 9.5 world.hazard_triggered

```ts
interface HazardTriggeredPayload {
  entity_id: string;
  hazard_type: string;
  new_status: string;
  description: string;
}
```

---

## 10. 编码落地要求

1. 后端先实现 Pydantic schema，再生成 OpenAPI。
2. 前端通过 `openapi-typescript` 生成 API 类型。
3. WebSocket payload 在前后端各保留一份测试样例。
4. 数据库模型字段名必须和 Pydantic schema 尽量一致。
5. 任何字段改名必须同步修改本文档。
