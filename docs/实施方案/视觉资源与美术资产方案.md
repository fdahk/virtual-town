# 视觉资源与美术资产方案

> 模块定位：确认建筑、人物、动物、地图、UI 等视觉资源的处理方式，保证最终产品视觉效果统一、可落地、可替换、可授权。

---

## 1. 结论

本项目不建议从零手绘所有建筑、人物和动物资源。原因是周期长、成本高、对美术能力要求高，容易拖慢核心 AI 小镇功能开发。

推荐策略是：

```text
MVP 阶段：使用统一风格的现成 2D 像素风资源包
  ↓
二次整理：统一尺寸、命名、动画帧、Tiled 图层
  ↓
项目资产化：形成本项目自己的 assets 目录和 asset manifest
  ↓
后续优化：关键角色和建筑再逐步定制
```

最终产品风格建议选择 **像素风 / top-down 2D / 星露谷式小镇风格**。

---

## 2. 为什么选择现成资源包

### 2.1 自己从零制作的问题

如果自己完整制作，需要覆盖：

- 室外地形：草地、道路、河流、桥、护栏、树木、花草。
- 建筑外观：咖啡店、学校、杂货店、住宅、诊所、花店。
- 室内资源：墙壁、地板、桌椅、柜台、床、沙发、咖啡机、货架。
- 人物：6 个 NPC + 玩家，多方向行走动画。
- 动物：2 狗 2 猫，多方向行走/待机/反应动画。
- 特效：对话气泡、情绪、落水、互动提示。

这些工作量远超 MVP 重点。项目核心价值是 AI Agent 驱动的小镇，而不是从零画完整美术。

### 2.2 使用现成资源包的优势

- 快速达到完整视觉效果。
- 风格统一。
- 可以直接导入 Tiled 和 Phaser。
- 留出时间做 Agent、记忆、交互、仿真闭环。
- 后续可替换为自制资源，不影响系统架构。

---

## 3. 资源获取策略

### 3.1 可用资源来源

优先从以下渠道寻找资源：

| 来源 | 适合资源 | 注意事项 |
|------|----------|----------|
| itch.io | 像素风角色、建筑、室内外 tileset | 必须查看 license，优先 CC0 / 可商用 |
| OpenGameArt | 免费开源游戏资源 | 风格可能不统一，要筛选 |
| Kenney.nl | 高质量免费游戏资源 | 风格偏通用，license 友好 |
| CraftPix / GameDev Market | 付费游戏资源 | 需确认购买授权 |
| 原 Stanford Generative Agents 项目资源 | 参考地图和资源组织 | 不建议直接用于正式交付，除非确认 license |

### 3.2 授权要求

正式项目资源必须满足至少一种条件：

1. CC0 / Public Domain。
2. 明确允许个人/商业项目使用。
3. 已购买授权。
4. 作者明确允许使用并保留授权说明。

禁止：

- 直接从商业游戏中截取资源。
- 使用未授权的《星露谷物语》原始素材。
- 使用来源不明的网络图片。
- 使用不允许二次分发的资源并提交到仓库。

---

## 4. MVP 推荐资源组合

### 4.1 资源风格

统一选择：

```text
2D top-down pixel art
tile size: 16x16 或 32x32
角色方向：至少 4 方向
动画：idle / walk 必须有，其他可选
```

### 4.2 必需资源清单

#### 地图 Tileset

必须包含：

- 草地。
- 土路/石板路。
- 水域/河流。
- 桥。
- 护栏。
- 树木。
- 花草。
- 建筑外墙和屋顶。
- 门和窗。

#### 室内 Tileset

必须包含：

- 墙。
- 地板。
- 桌椅。
- 柜台。
- 床。
- 沙发。
- 书架。
- 货架。
- 咖啡机。
- 诊所/学校相关物品。

#### 人物 Sprite

至少需要：

- 玩家默认角色 1 套。
- 6 个 NPC 角色。
- 每个角色至少 4 方向行走动画。

#### 动物 Sprite

至少需要：

- 狗 2 种颜色或造型。
- 猫 2 种颜色或造型。
- 每个动物至少 idle / walk 动画。
- 如果资源支持，额外使用 happy / scared / sleep 动画。

#### UI 资源

可以先使用 React 组件库，不强依赖像素风 UI。但建议有：

- 对话气泡。
- 互动提示图标。
- 情绪 emoji / icon。
- 状态图标，例如溺水、饥饿、开心、害怕。

---

## 5. 资源建模方式

### 5.1 AssetManifest

所有资源进入项目后，必须登记到 manifest，避免前端硬编码路径。

```json
{
  "id": "sprite_npc_xiaofang",
  "asset_type": "sprite",
  "entity_type": "human",
  "url": "/assets/sprites/humans/xiaofang.png",
  "frame_width": 32,
  "frame_height": 48,
  "animations": {
    "idle_down": { "frames": [0], "frame_rate": 1 },
    "walk_down": { "frames": [0, 1, 2, 3], "frame_rate": 8 },
    "walk_left": { "frames": [4, 5, 6, 7], "frame_rate": 8 },
    "walk_right": { "frames": [8, 9, 10, 11], "frame_rate": 8 },
    "walk_up": { "frames": [12, 13, 14, 15], "frame_rate": 8 }
  },
  "license": {
    "name": "CC0",
    "source": "https://example.com",
    "author": "asset_author"
  }
}
```

### 5.2 TilesetManifest

```json
{
  "id": "tileset_town_outdoor",
  "url": "/assets/tilesets/town_outdoor.png",
  "tile_width": 16,
  "tile_height": 16,
  "columns": 32,
  "source": "itch.io asset pack",
  "license": "CC0 or purchased"
}
```

### 5.3 Entity Visual

数据库中的 Agent 不直接写死图片路径，而是引用 asset id。

```json
{
  "agent_id": "npc_xiaofang",
  "visual": {
    "sprite_asset_id": "sprite_npc_xiaofang",
    "portrait_asset_id": "portrait_npc_xiaofang",
    "scale": 1,
    "default_direction": "down"
  }
}
```

---

## 6. 资源目录规范

```text
frontend/public/assets/
├── maps/
│   ├── town_outdoor.tmj
│   ├── hobbs_cafe_inside.tmj
│   ├── school_inside.tmj
│   └── grocery_inside.tmj
├── tilesets/
│   ├── outdoor/
│   ├── interior/
│   └── objects/
├── sprites/
│   ├── humans/
│   ├── dogs/
│   ├── cats/
│   └── player/
├── portraits/
│   ├── humans/
│   └── animals/
├── effects/
├── ui/
└── manifest/
    ├── assets.manifest.json
    ├── sprites.manifest.json
    └── tilesets.manifest.json
```

---

## 7. Tiled 中的资源使用规则

### 7.1 Tile Size

MVP 推荐：

```text
地图逻辑 tile：32x32
原始资源 tile：16x16 或 32x32
如果原始资源是 16x16，可在 Phaser 中 scale 到 2x
```

统一约束：

- 同一张地图内 tile size 必须一致。
- 不同 tileset 可以来自不同资源包，但视觉风格必须接近。
- 室外和室内资源可以分开，但角色比例必须一致。

### 7.2 Object Layer

Tiled 中所有可交互对象必须带属性：

```json
{
  "object_id": "obj_cafe_coffee_machine",
  "object_type": "facility",
  "blocks_movement": true,
  "interactions": "make_coffee,inspect,clean"
}
```

这些属性会被导入脚本写入数据库。

---

## 8. 资源导入工具

需要实现一个资源导入脚本：

```bash
python -m app.tools.import_assets \
  --asset-dir frontend/public/assets \
  --manifest frontend/public/assets/manifest/assets.manifest.json
```

职责：

1. 校验资源文件是否存在。
2. 校验 manifest 字段完整。
3. 校验 license 信息是否填写。
4. 将 asset metadata 写入数据库。
5. 校验 Tiled map 引用的 tileset 路径是否有效。

---

## 9. 美术统一策略

### 9.1 风格原则

- 优先 pixel art。
- 角色比例统一。
- 建筑和室内家具透视统一。
- 动物尺寸与角色比例合理。
- 不混用明显冲突的高清手绘、卡通、像素资源。

### 9.2 颜色原则

- 小镇整体偏温暖、明亮。
- 危险区域要视觉上可识别，例如深色河流、警示标识。
- 可交互对象可以通过 hover 高亮或描边提示。

### 9.3 后续定制

MVP 后可定制：

- 主要 NPC 头像。
- 关键建筑招牌。
- 玩家角色换装。
- 特殊事件动画。

---

## 10. 原版资源是否可直接使用

原 Stanford Generative Agents 项目中的资源可以作为学习参考，尤其是：

- 地图结构。
- Tiled 图层组织。
- 人物 sprite 使用方式。
- 室内外小镇布局思路。

但不建议在正式交付中直接依赖原项目资源，除非已经确认其授权允许当前用途。

原因：

- 原项目资源来源涉及多个艺术作者。
- 不同资源可能有不同授权。
- 直接使用会让项目辨识度不足。

推荐做法：

1. 参考原项目布局和资源组织方式。
2. 自己选择明确授权的像素风资源包。
3. 用 Tiled 重新搭建本项目地图。
4. 在 README 或 credits 中记录资源来源和 license。

---

## 11. MVP 资源采购/整理任务

第一阶段必须完成：

1. 确定一个室外 tileset。
2. 确定一个室内 tileset。
3. 确定至少 6 个人类角色 sprites。
4. 确定至少 2 狗 2 猫 sprites。
5. 确定基础 UI icon / emoji 方案。
6. 建立 asset manifest。
7. 在 Tiled 中制作：
   - 室外小镇地图。
   - 咖啡店内部。
   - 学校内部。
   - 杂货店内部。
8. 记录所有资源 license。

---

## 12. 验收标准

1. 所有视觉资源都有明确来源和 license。
2. 所有 sprite 都能在 Phaser 中播放 idle / walk 动画。
3. Tiled 地图能正常加载，tileset 路径不丢失。
4. 室外和室内地图风格统一。
5. 人类、狗、猫、玩家的视觉比例合理。
6. 资源路径通过 manifest 引用，不在业务代码中硬编码。
7. README 或 credits 能说明资源来源。
