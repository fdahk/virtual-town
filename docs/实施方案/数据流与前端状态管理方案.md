# 数据流与前端状态管理方案

> 模块定位：统一前端数据获取、缓存、运行态同步、Phaser 与 React 通信，避免数据流混乱。

---

## 1. 设计目标

本项目同时存在 REST、WebSocket、React UI、Phaser 游戏场景和本地状态。如果没有统一数据流规范，后续会很容易出现：

- 某些组件自己请求后端。
- Phaser 和 React 各自维护一套角色位置。
- WebSocket 消息和 REST 查询结果互相覆盖。
- UI 状态、服务端状态、游戏渲染状态混在一起。

因此必须明确数据分层和单向数据流。

---

## 2. 数据分层

| 数据类型 | 来源 | 前端存储 | 说明 |
|----------|------|----------|------|
| 服务端权威数据 | REST API | TanStack Query | Agent 详情、地图、记忆、关系、配置 |
| 实时运行态 | WebSocket | Zustand runtime slice | 坐标、当前状态、事件增量 |
| UI 临时状态 | 用户操作 | Zustand ui slice / React state | 当前选中 NPC、面板展开、调试层 |
| Phaser 渲染态 | Zustand + EventBus | Phaser Scene 内部 | sprite 实例、动画播放、摄像机 |
| 表单状态 | 用户输入 | React Hook Form | 角色创建、配置编辑 |

---

## 3. 数据流原则

```text
后端 REST / WebSocket
        ↓
api 层 / websocket client
        ↓
TanStack Query / Zustand
        ↓
React 组件 / EventBus
        ↓
Phaser 渲染
```

禁止反向依赖：

- React 组件禁止直接 `fetch`。
- Phaser 禁止直接请求后端。
- Phaser 禁止直接修改 React 组件 state。
- 业务组件禁止直接解析 WebSocket 原始消息。

---

## 4. 前端目录约束

```text
frontend/src/
├── api/
│   ├── client.ts
│   ├── agents.ts
│   ├── world.ts
│   ├── simulation.ts
│   └── players.ts
├── websocket/
│   ├── client.ts
│   └── handlers.ts
├── stores/
│   ├── runtimeStore.ts
│   ├── uiStore.ts
│   └── gameBridgeStore.ts
├── game/
│   ├── eventBus.ts
│   └── systems/
└── hooks/
    ├── useAgents.ts
    ├── useWorld.ts
    └── useSimulation.ts
```

---

## 5. REST 数据获取

组件不能直接调用 API 文件，必须通过 hooks：

```ts
function useAgents() {
  return useQuery({
    queryKey: ["agents"],
    queryFn: agentApi.listAgents,
  });
}
```

组件使用：

```tsx
const { data: agents } = useAgents();
```

禁止：

```tsx
useEffect(() => {
  fetch("/api/agents");
}, []);
```

---

## 6. WebSocket 数据处理

WebSocket 原始消息只允许在 `websocket/handlers.ts` 中解析。

```ts
function handleSimulationDelta(payload: SimulationDeltaPayload) {
  runtimeStore.getState().applyDelta(payload);
  gameEventBus.emit("simulation.delta", payload);
}
```

组件和 Phaser 都不直接解析原始消息。

---

## 7. Zustand Store 划分

### 7.1 runtimeStore

保存实时运行态：

```ts
interface RuntimeStore {
  worldTime: string;
  step: number;
  agentsById: Record<string, AgentRuntimeState>;
  recentEvents: WorldEvent[];
  applyDelta: (delta: SimulationDeltaPayload) => void;
}
```

### 7.2 uiStore

保存 UI 状态：

```ts
interface UIStore {
  selectedEntityId?: string;
  rightPanelOpen: boolean;
  debugLayers: {
    collision: boolean;
    hazard: boolean;
    portal: boolean;
  };
}
```

---

## 8. Phaser 通信

Phaser 只能通过 EventBus 接收指令：

```ts
gameEventBus.emit("agent.move", {
  agentId: "npc_xiaofang",
  to: { x: 12, y: 18 },
});
```

Phaser 点击实体后，也通过 EventBus 通知 React：

```ts
gameEventBus.emit("agent.selected", {
  agentId: "npc_xiaofang",
});
```

---

## 9. 数据写入命令

玩家行为必须封装成命令函数：

```ts
playerCommands.moveTo(tile);
playerCommands.interact(targetId, interactionType);
dialogueCommands.sendMessage(targetAgentId, text);
```

组件禁止自己拼请求：

```tsx
await fetch("/api/players/me/move", ...); // 禁止
```

---

## 10. 验收标准

1. 全项目搜索不到组件内直接 `fetch` 后端 API。
2. Phaser 不直接请求后端。
3. WebSocket 原始消息只有一个统一入口。
4. 服务端状态用 TanStack Query，运行态用 Zustand。
5. 玩家行为通过 commands 层发起。
6. 前端刷新后能通过 REST 快照恢复，随后继续接收 WebSocket delta。
