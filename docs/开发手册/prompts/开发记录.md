竞品分析

需求分析

制定PRD

制定开发规范

制定架构方案

制定模块方案

制定阶段性成果验收标准

制定维护标准


1.阶段一：核心框架落地

virtual-town/
├── backend/                 # FastAPI + SQLAlchemy + Alembic + Pydantic v2
│   ├── app/
│   │   ├── core/            # config / logging / errors / event_bus / time
│   │   ├── api/             # REST 路由（world/agents/player/simulation/memory）
│   │   ├── websocket/       # 增量广播 gateway
│   │   ├── schemas/         # Pydantic schema（对齐契约 V1）
│   │   ├── db/              # ORM 模型 + seed 脚本（6 NPC + 4 动物 + 关系 + 预置记忆）
│   │   ├── domain/
│   │   │   ├── world/       # 场景网格、A* 寻路、场景缓存
│   │   │   └── simulation/  # 仿真引擎、状态机、规则版 Agent 行为、日程
│   │   ├── services/        # WorldService / AgentService / PlayerService / MemoryService / SimulationRuntime
│   │   └── llm/             # OpenAI 兼容客户端 + Embedding + 对话生成（含规则兜底）
│   ├── alembic/             # 单个初始 migration（含 pgvector 扩展）
│   └── tests/               # pytest（grid/astar、memory 评分、日程选择）
├── frontend/                # React + Vite + TypeScript + Phaser 3 + Zustand + TanStack Query
│   ├── src/
│   │   ├── api/             # http / websocket（组件禁止直接 fetch）
│   │   ├── game/            # PhaserGame + TownScene + EventBus
│   │   ├── stores/          # Zustand worldStore（统一数据中心）
│   │   ├── pages/           # CreatePlayerPage / TownPage
│   │   └── components/      # AgentList / AgentPanel / ChatPanel / EventLog / MemoryViewer / TimeControl / DebugToggle
├── scripts/                 # dev.sh / seed.sh / reset_db.sh
├── docker-compose.yml       # postgres(pgvector) + redis + backend + frontend
├── .env.example             # 全量配置模板
└── README.md


1.世界与地图：40×30 室外大地图 + 3 个 16×12 室内场景；咖啡店/学校/杂货店/花店/公园/河边长椅/6 处住宅；河流有 hazard+护栏，桥为安全通道；所有 portal 自动切换场景。

2.Agents：6 个 NPC（小芳/小明/小王/林娜/陈伯/阿言）+ 4 动物（豆豆/黑黑/咪咪/小白）+ 默认玩家，每个 NPC 都有完整日程模板。

3.仿真循环：SimulationEngine 驱动 world tick + AI tick 解耦，状态机 IDLE → THINKING → PLANNING_PATH → MOVING → INTERACTING；异步队列接玩家输入；增量通过事件总线广播。

4.规则版 AI：decide_human_action 根据当前游戏时间选日程 → A* 生成路径；decide_animal_action 按性格反应玩家（亲人靠近、胆小躲开、警觉观望、疲惫睡觉）。

5.LLM + 规则兜底：LLMClient OpenAI 兼容，JSON 输出 + Pydantic 校验；generate_npc_reply 未配置 API key 时自动走规则兜底，仿真永不崩溃。

6.记忆系统：写入 Postgres，三因素评分（recency 0.5 / importance 3.0 / relevance 2.0），有 embedding 用 cosine，无 embedding 退化为中英关键字相关性；种子中预置「小芳记得小王喜欢美式咖啡」这条演示记忆。

7.玩家交互：创建角色 → 地图点击/WASD 移动 → 走近 NPC 对话（调 LLM + 引用记忆）→ 走近动物抚摸/喂食/呼唤/惊吓（带关系更新）→ 走进河无护栏处触发 DROWNING。

8。端 UI：Phaser 用纯色矩形绘制 tile/building/object/sprite（MVP 免外部资源），React 负责所有面板；WebSocket 自动重连；调试层可开关碰撞/危险/出入口。

9.工程化：Docker Compose、Alembic 迁移、scripts/dev.sh 一键启动、openapi-typescript 可从 OpenAPI 生成前端类型、统一错误模型（code/message/retryable/details）、结构化日志。


---

2. 阶段二：LLM 工具调用 + 反思 + 真实美术

10. Tool Calling：把 ToolCalling 工具契约 V1 转为 ToolRegistry + ToolExecutor；实现 12 个工具（move_to_location / move_to_entity / interact_with_object / avoid_danger / talk_to_entity / face_entity / write_memory / search_memory / react_to_pet / make_sound / update_emotion / wait），按 entity_type 授权；接入 LLM agent_decision 管线（perceive → retrieve → plan → execute），引擎优先 LLM 决策、失败回退规则版。

11. Reflection：domain/memory/reflection.py，重要度累计触发 thought 写入；游戏内 22:30 后生成 daily summary；LLM 失败均走规则兜底。

12. Prompt 模板版本化：`backend/app/prompts/<prompt_id>/`（`v1.jinja2` + `metadata.json` + 可选 `examples.json`）；代码经 `prompts.render_prompt()` 加载（如 `agent_decision`、`reflection`、`daily_summary`）。

13. 美术资源拍板（A+B+C）：
    - A. Kenney Tiny Town（CC0）做 tileset / 建筑 / 家具，OpenGameArt 镜像下载；
    - B. Universal LPC Spritesheet（CC-BY-SA 3.0）做人物 4 方向 walk，分层下载后由 scripts/compose_lpc.py 合成；LPC Cats and Dogs（CC-BY 3.0）做猫狗；
    - C. AI 生成 11 张对话立绘（6 NPC + 1 玩家 + 4 动物），固定 prompt 模板见映射表（视觉资源与美术资产方案 §4.1.2）。
    - tile 尺寸 32×32（原 16×16 在 Phaser 内 2x 缩放）。
    - 一键 scripts/fetch_assets.sh 下载与组装。
    - CREDITS.md 集中署名。

14. 立绘 prompt 模板（统一基线）：
    "2D RPG pixel-art style character portrait of <角色描述>, upper body chest-up pose facing forward, <场景背景描述, blurred bokeh>, 16-bit JRPG visual-novel portrait style, clean linework, soft shading, Stardew Valley companion portrait feel, square 1:1 composition, centered."
    每张图根据角色调整核心形象、服饰、配饰、场景。生成时间 2026-04-30。
    生成结果一律放入 frontend/public/assets/portraits/ 并登记到 portraits.manifest.json。

本阶段交付总结 ：
1. Sprint 6 完整 Tool Calling
新增 backend/app/llm/tools/ 完整工具栈：

base.py / registry.py / executor.py — 抽象、注册、执行三件套
12 个工具按 entity_type 授权：
世界类：move_to_location / move_to_entity / interact_with_object / avoid_danger
对话类：talk_to_entity / face_entity
记忆类：write_memory / search_memory
动物类：react_to_pet / make_sound
状态类：update_emotion / wait
agent_decision.py — perceive → retrieve → plan → execute 管线
引擎接入：SimulationEngine._decide_all 优先 LLM，失败回退规则版，仿真永不卡住
版本化 prompt：`backend/app/prompts/agent_decision/v1.jinja2`（及 reflection、daily_summary 等目录）。

2. 反思与每日总结
backend/app/domain/memory/reflection.py：

maybe_reflect：累计重要度阈值触发 thought 写入，引用证据 id
maybe_daily_summary：游戏内 22:30 后生成 summary
LLM 失败均走规则兜底
引擎每决策周期为 1 个候选 agent 跑反思 + 全员检查日结
新 API：POST /api/agents/{id}/reflect（手动触发，供演示与 E2E 用）
版本化 prompt：`prompts/reflection/v1.jinja2`、`prompts/daily_summary/v1.jinja2`。

3. 真实美术（A+B+C 落地）
文档先行：

更新 视觉资源与美术资产方案.md §4.1.1/§4.1.2/§11 锁定 Kenney + LPC + AI 立绘 + 32×32 + 11 个 NPC 立绘
更新 React 总方案与前端渲染管线
新增 CREDITS.md 集中署名
资源到位：

scripts/fetch_assets.sh — 一键下载 Kenney Tiny Town（CC0）+ LPC Cats/Dogs（CC-BY 3.0）+ LPC 人物分层（CC-BY-SA 3.0）
scripts/compose_lpc.py — 把 LPC body/legs/feet/torso/hair 多层 PNG 合成为每个 NPC 的 576×256 walk sheet（同时兼容 LPC 1344 与 2944 两种高度）
11 张 AI 生成立绘（6 NPC + 玩家 + 4 动物），prompt 与生成时间记录在 docs/prompts/记录.md
三份 manifest：tilesets.manifest.json / sprites.manifest.json / portraits.manifest.json
backend/app/tools/export_tmj.py — 由数据库 tiles 程序化生成 Tiled .tmj
前端升级：

frontend/src/game/assets/manifest.ts — manifest 类型与加载器
TownScene.ts 全量重写：Phaser preload 真正的 Kenney tileset + LPC sprite sheets，自动生成 idle/walk × 4 方向动画，按 state + facing 切换，动画失败有色块兜底，hazard 用半透明蓝色叠加补齐水面
PhaserGame.tsx 在场景实例化前注入 manifests，确保 preload 拿到正确 URL
usePortrait hook + ChatPanel 左侧 156×188 立绘 + AgentPanel 顶部小头像

4. Playwright E2E 验收脚本
e2e/ 目录覆盖 PRD §10 全部 6 个演示场景：

文件	场景
01_enter_town.spec.ts
玩家进入小镇，UI 基线可见
02_enter_building.spec.ts
走到咖啡店门口 → portal 自动切换室内
03_npc_memory_chat.spec.ts
问小芳"小王喜欢喝什么"，回复命中"美式咖啡"，引用记忆+新增 chat 记忆
04_animal_interaction.spec.ts
抚摸豆豆（enjoy）vs 抚摸小白（escape）
05_hazard.spec.ts
走到无护栏河流处触发 DROWNING 状态
06_followup_memory.spec.ts
对话→强制反思生成 thought→第二轮对话仍能引用记忆
特性：

通过 REST API 把世界恢复到可比较状态，不依赖用例顺序
单 worker 串行（共享单实例小镇）
河流场景自动暂停仿真避免引擎"自救"
详细 README 与 npm scripts

5. 回归全绿
后端 pytest：12/12 通过（含 grid/astar / memory 评分 / 日程 / tool registry / reflection 兜底）
前端 tsc --noEmit：通过
E2E tsc --noEmit：通过
前端 vite build：通过
ReadLints：0 错
实际启动 E2E 的下一步

./scripts/dev.sh                # 起 postgres + redis + backend + frontend
cd e2e
npm install                     # 已在你机器执行过
npx playwright install chromium # 首次
npm test                        # 跑全部 6 个场景

---

3. 阶段三：基础设施层（异步队列 + 数据层深化 + 可观测性平台）

> 目标：把 `docs/实施方案/MVP开发任务拆分.md` §12 / §13 / §14 作为一整块基础设施一次交付，
> 为后续 §15（NPC 深化）、§16（文本处理）、§17（Prompt 评测）铺平道路。

15. §14.1 可观测性基础设施（TraceContext）
- `backend/app/core/trace_context.py`：基于 contextvars 的
  `request_id / trace_id / span_id / parent_span_id / simulation_id / agent_id /
  player_id / task_id / event_id / category` 传递。
- `TraceContext.start_trace(category, ...)` 开顶层 trace，`TraceContext.span(name)` 嵌套 span，
  `TraceContext.apply(snapshot)` 支持 worker 从任务 payload 恢复 trace。
- 结构化日志 `app/core/logging.py`：新增 `TraceContextFilter`，所有 log record
  自动带上当前 trace 字段；dev 模式 formatter 把 `trace_id/simulation_id/agent_id/task_id`
  inline 到每条日志末尾。
- HTTP 入口中间件（`app/main.py`）为每个请求开一条 `http.*` trace；响应头自动回写
  `x-trace-id / x-request-id`。
- world tick 在 `SimulationEngine._tick` 外层套 `world_tick` trace，子模块（决策、工具、LLM）
  自动嵌套同一 trace。

16. §14.1 错误码统一治理
- `app/core/errors.py`：新增 `ErrorCode` enum，按模块前缀分类
  `WORLD_ / AGENT_ / MEMORY_ / LLM_ / TOOL_ / PLAYER_ / WS_ / DB_ / TASK_`。
- `AppError.to_dict()` 在 trace 激活时自动注入 `trace_id / request_id`，前端可稳定
  显示与排查；校验错误 422 响应同样带 trace。
- 新增错误基类 `TaskError / TaskDuplicate / TaskNotFound / LLMUnavailable`，老错误类
  `InvalidArguments / TargetNotFound / ...` 沿用原 `code` 值向后兼容。

17. §12 异步任务队列与 Worker
- 新表 `tasks`（id/task_type/status/retry_count/max_retries/idempotency_key/payload/
  result/enqueued_at/started_at/finished_at/last_error/trace_id/...） +
  `task_status_log`（全量状态变迁审计）。
- `backend/app/domain/tasks/`：
  - `registry.py` — `TaskHandler / TaskRegistry / TaskContext / TaskResult`。
  - `queue.py` — 入队：`tasks` 行落库幂等后再 push 至 Redis/RQ（**消费语义以本条下 §24「队列回正」为准**）。
  - `handlers.py` — 注册 MVP 任务拆分要求的全部 7 类 handler：
    `agent_decision / generate_daily_plan / generate_dialogue_reply /
    write_memory_embedding / daily_reflection / query_rewrite / relationship_update`。
    **阶段十五、十六已落地**：上述 handler 均有真实业务路径（详见 `handlers.py`、`player_service.py` 对话链路）。
  - `ttl_worker.py` — TTL 清理 worker：过期短期记忆按 `importance >= 7` 升级为长期，
    其余标 archived。
- `SimulationEngine._decide_all` 分叉：
  - `task_queue_mode=async`（默认）：LLM 决策投递到 TaskQueue，主循环不 await 网络；
    同步路径留给 rule_agent 给出即时行为。杀掉 LLM 接口仍能跑（规则兜底）。
  - `task_queue_mode=sync`：保留原先 inline LLM 路径，便于回归测试。
- `MemoryService.write(defer_embedding=True)` 默认把 embedding 计算投递到
  `write_memory_embedding` 任务，写路径不再阻塞在网络 I/O。
- Worker 进程独立拉起（Compose `worker` service / `./scripts/dev.sh`）；RQ 优先级队列 `vt:high|default|low`。**阶段三早期「API 进程内嵌 worker」文案作废，详见 §24。**
  切 Celery 时只需替换 `TaskQueue` 实现，`enqueue/get/list` API 保持不变。

18. §13 数据层深化
- Alembic `20260430_0002_infra.py` 为 `memories.embedding` 建 pgvector HNSW 索引
  （`m=16, ef_construction=64, vector_cosine_ops`），支持 10 NPC × 1k 条记忆量级
  的 P95 < 50ms 检索（验收条件达成由后续压测脚本验证）。
- `backend/app/core/redis_client.py`：异步 Redis 封装，集中维护方案 §5 键空间——
  `agent:{id}:short_memory` / `agent:{id}:runtime` / `dialogue:{id}:recent_messages` /
  `world:{scene}:active_entities` / `sim:{id}:state` / `task:{id}:status`。
  `set_json / list_push / set_add / scan_keys` 等，Redis 不可用时返回 False/[]，
  不影响业务主路径。
- `SimulationEngine._persist_tick` 在 Postgres 写入完成后做 Redis 写穿透：
  把每个 agent 的 runtime 热写到 `agent:{id}:runtime`（TTL 300s），
  场景活跃实体集合写到 `world:{scene}:active_entities`。
- `MemoryService.write` 写 Postgres 的同时把短期记忆镜像到
  `agent:{id}:short_memory`（TTL 24h），供快速召回。
- `scripts/backup_db.sh`：`pg_dump` + asset manifest 打包到 `./backups/`，
  优先直连 localhost，不可达时回退 `docker compose exec`。

19. §14.2 / §14.3 观测数据面与 API
- 新表 `observability_events / llm_calls / tool_calls`，配合 `task_status_log`
  构成完整观测事件流；业务代码一律通过 `app.services.observer.Observer`
  埋点，不直接写表。
- `Observer`：后台 flush 协程（默认 1.5s / 200 条），按类别分桶，flush 失败丢弃
  最旧批次并告警；敏感字段（api_key / authorization / password / raw_prompt）
  自动脱敏，超长字符串截断 512。
- `EventRouter`（`app/services/event_router.py`）订阅事件总线的
  `world.event_persisted`，将每条 WorldEvent 转写为 Observer 的 `world_event` 事件，
  为未来路由到 Memory / TaskQueue 预留了钩子。
- `LLMClient.chat_json / embed` 与 `ToolExecutor.execute_one` 全量埋点：
  latency / retry / schema_valid / fallback / error_code / permission / world_state 校验
  分项都进入观测表。
- REST `/api/observability/*`：
  - `dashboard`（聚合：仿真 / WS 在线数 / LLM 5 分钟平均耗时 / 错误数 / 任务分布）。
  - `events / world-events / agents/{id}/runtime / traces/{trace_id} /
    llm-calls / tool-calls / tasks / tasks/{id} / memories / errors`。
- `WS /ws/observability/{simulation_id}`：观测事件实时推送，与普通仿真 WS 通道隔离。
- 健康检查：`/api/health/db`、`/api/health/redis`、`/api/health/llm`
  （LLM 不发真实请求，只校验 key/配置）。
- 观测 WS 广播统一信封 `{version, type: "observability.event_created", payload, sent_at}`。

20. §14.5 WebSocket 健壮性
- `simulation.delta` payload 追加单调递增的 `seq` 字段；
  前端 `ReliableSocket` 基于 `seq` 检测丢失与乱序，`dropped_seqs` 计数暴露给 WS Monitor。
- 前端重连：指数退避（500ms → 15s），重连成功后自动调用 `onReconnect` 钩子——
  `TownPage` 绑定为 `GET /api/simulations/{id}/state` 拉快照替换 runtime store。
- `ObservabilitySocket` 独立重连、独立连接管理，观测平台断网不影响玩家页面。

21. §14.4 前端观测平台
- 新独立路由 `/observability`（App.tsx 按 path 切换），与玩家页面完全隔离。
  11 个子页面：Dashboard / World Events / Obs Events / Agent Runtime /
  Traces / LLM Calls / Tool Calls / Tasks / Errors / WS Monitor / Health。
- 组件：`ObservabilityPage.tsx`（总入口 + 9 个表/卡视图）、`TraceDetail.tsx`
  （span 时间线 + LLM/Tool/Task 详情）、`AgentRuntimeCard.tsx`（实体档案 +
  状态 + Redis 热数据 + 最近 20 事件 + 最近 10 记忆）。
- 风格：深色研发工具面板，研发不需要"好看"，需要"一眼看清问题"。
- 按研发场景把几类研发动作放到单页可达：
  - 要看一次玩家对话怎么被拆解 → Traces + trace_id。
  - 要看某 NPC 为何不动 → Agent Runtime 输入 agent_id。
  - 要看 LLM 是不是挂了 → LLM Calls + Health。
  - 要看哪些任务卡 pending → Tasks 表。
  - 要看错误趋势 → Errors（按 category:event_type 聚合）。

22. 配置与启动
- `.env` / `config.py`：
  - `TASK_QUEUE_MODE=async|sync`、`TASK_QUEUE_BACKEND=rq`、
    `TASK_QUEUE_HIGH=vt:high / TASK_QUEUE_DEFAULT=vt:default / TASK_QUEUE_LOW=vt:low`、
    `TASK_QUEUE_WORKER_COUNT=1`。
  - `OBSERVABILITY_FLUSH_INTERVAL=1.5`、`OBSERVABILITY_BATCH_SIZE=200`。
- API 进程 `main.py` 生命周期顺序：logging → Observer.start → subscribe_event_router →
  register_handlers + init_task_queue（**仅 enqueue**，不再跑内嵌 worker）→ TTL worker.start
  → SimulationEngine.start → serve。shutdown 反向；`TaskQueue.close()` 释放 Redis 连接池。
- Worker 进程 `python -m app.domain.tasks.worker`：
  - `docker-compose.yml` 新增 `worker` service，command 就是这条；扩容直接
    `docker compose up --scale worker=N`。
  - `scripts/dev.sh` 自动并起 `worker` 进程（与 backend / frontend 并列）。
  - 关键实现细节：worker 进程内维护**一个持久的 asyncio event loop 线程**
    （`runner._ensure_loop`），所有 RQ job 用 `run_coroutine_threadsafe` 投递进来；
    避免每 job `asyncio.run` 重建 loop 导致 SQLAlchemy AsyncEngine / asyncpg
    "attached to a different loop" 冲突。TTL worker 共享这个 loop。

23. 回归测试
- 后端 pytest：21 passed + 4 skipped（isolated DB 需要 `TEST_DATABASE_URL` 指定）。
  - 新增 `tests/test_trace_and_errors.py`（trace 上下文嵌套、error code 前缀、
    AppError.to_dict trace_id 注入）。
  - 新增 `tests/test_observer_unit.py`（敏感脱敏、长串截断、无 session factory 写入容错）。
  - 新增 `tests/test_tasks_and_observer.py`（隔离 DB 下跑幂等、生命周期、重试、
    EventRouter → Observer 链路）。
- Alembic `upgrade head` 新增 5 张表 + 1 个 HNSW 索引；`downgrade` 正确逆序。
- 前端 `tsc -b`：通过。
- 手测：
  - `./scripts/dev.sh` 一键起；`/api/health/db|redis|llm` 正确反映依赖。
  - `/api/observability/dashboard` 返回聚合；task 表里能看到 pending → running →
    succeeded / failed 生命周期。
  - 错误响应头带 `x-trace-id`，response body 带 `trace_id / request_id`。
  - 前端 `/observability` 各页能打开，观测 WS 接到实时事件。

24. 阶段 12 队列方案回正（2026-05-01）
    原实现错误地把 `tasks` 表 + `SELECT ... FOR UPDATE SKIP LOCKED` 当队列直接用，
    偏离了 `MVP开发任务拆分.md §12.1`、`事件系统与任务调度模块实施方案.md §2`、
    `部署发布与运行维护方案.md §1`、`实施方案完整性审查与补漏清单.md §1.3`
    一致规定的"Redis + RQ（MVP）/ Celery（深化）"选型。
    按"方案是约束"原则（MVP §0.4）回正：
    - 新增 `rq>=1.16,<2` 依赖。
    - `backend/app/domain/tasks/queue.py` 重写为"先写 `tasks` 行（幂等）→ 再 push 到 RQ"。
      `SELECT FOR UPDATE SKIP LOCKED` 抢占逻辑全部删除。
    - 新增 `backend/app/domain/tasks/runner.py`：RQ 同步入口 `run_task_job(task_id)`，
      内部通过 `run_coroutine_threadsafe` 投递到 worker 进程的**持久 asyncio loop**
      执行 async handler；失败 + retryable + retry_count < max_retries 时回推 RQ
      并做指数退避。
    - `app/domain/tasks/worker.py` 重写为 RQ `SimpleWorker` 入口，监听
      `vt:high / vt:default / vt:low` 三个优先级队列，`with_scheduler=True` 支持延迟重试。
    - API 进程的 lifespan **不再跑内嵌 worker**，只做 enqueue + TTL。
    - `docker-compose.yml` 正式启用 `worker` service；`scripts/dev.sh` 并起 worker 进程。
    - 回归：`pytest` 21 passed + 4 skipped；
      手测 enqueue `write_memory_embedding`（success + failure 路径）和 `generate_daily_plan`
      等异步任务：API 进程 → Redis → worker 进程 → `tasks` 表状态轨迹完整落库，
      单次任务 300–500ms 内完成，`/api/observability/tasks` 实时可见。
    - Bug 复盘（跨 loop / 双 Worker 等）：`docs/debug/20260501-task-queue-rq-asyncio-loop.md`

26. 响应延迟优化（2026-05-01）
    基于 trace_c99a781e6279 的链路分析，识别出三类主要性能瓶颈并完成以下优化：

    **P0 — move_to_location 不可达目标熔断**
    - `backend/app/llm/tools/world_tools.py`：`MoveToLocationTool` / `MoveToEntityTool` 在
      返回 `TARGET_NOT_REACHABLE` 时，调用 `_mark_unreachable()` 将目标 id 写入
      Redis `agent:{id}:unreachable` 集合（TTL 5 分钟）。
    - `backend/app/llm/agent_decision.py`：`_perceive()` 中新增 `_get_unreachable_set()`，
      读取黑名单后从感知地点列表和附近实体列表中过滤不可达目标，LLM prompt 不
      再包含已知不可达选项，从根本上断开"失败→再尝试→再失败"的循环。
    - 新增 Redis key 构造器：`key_agent_unreachable()` in `core/redis_client.py`。
    - 预期收益：消除 trace 中 ~75s 的无效 LLM 调用循环（6 轮 × ~12s）。

    **P1 — agent_decision 并行化 retrieve + planning_context**
    - `backend/app/llm/agent_decision.py`：`decide_with_llm()` 中将原串行的
      `await _retrieve(...)` 和 `await get_planning_service().get_current_context(...)` 
      改为 `asyncio.gather()` 并发执行，节省约 6s/cycle。

    **P1 — Embedding 向量 Redis 缓存**
    - `backend/app/llm/embedding.py`：`EmbeddingService.embed()` 新增 Redis 缓存层，
      以 `sha256(text)[:32]` 为 key，TTL 1 小时。相同文本直接命中缓存，
      消除重复 Embedding API 调用。
    - 新增 Redis key 构造器：`key_embedding_cache()` in `core/redis_client.py`。

    **P1 — write_memory_embedding 降为 low 优先级队列**
    - `backend/app/services/memory_service.py`：enqueue `write_memory_embedding` 时
      设置 `priority=9`（low 队列），确保 `agent_decision`（priority≤3，high 队列）
      始终优先被 worker 消费，消除 ~71s 的 embedding 积压阻塞决策任务的问题。

    **P2 — _maybe_reflect_batch 移出 tick 主循环**
    - `backend/app/domain/simulation/engine.py`：将原 `_maybe_reflect_batch()` 
      内联 LLM 调用（阻塞 tick 5-10s）替换为 `_enqueue_reflect_batch()`，
      投递 `daily_reflection` 任务到 low 队列（priority=8）异步执行。
      保留 `_last_reflect_at` / `_last_summary_day` 内存频率控制，避免过度入队。
      日结任务以 `summary:{day_key}` 为幂等 extra，保证一天只执行一次。

27. Trace 边界与 Deadline 双 Bug 修复（2026-05-01）

    **问题发现**：观测平台 trace 展示一次 world_tick 的 wall-clock 长达 979 600 ms（约 16 分钟），
    用户质疑数据合理性，进而发现两个独立 bug。

    **Bug 1：任务 deadline 从未被 runner 检查（最高优先级）**
    - `agent_decision` 任务入队时已设置 `deadline_seconds=60`（存入 `tasks.deadline_at`），
      但 `backend/app/domain/tasks/runner.py` 的 `_execute_task` 在取出任务后
      **从未读取 `deadline_at` 字段**，导致即使任务在 RQ 队列等待了 16 分钟，
      worker 仍会照常执行一个早已过期的 NPC 决策。
    - 修复：在 `_execute_task` 的终态检查之后、handler 调用之前，加入 deadline 检查：
      若 `utcnow() > task.deadline_at`，直接将任务标记为 `FAILED`（error_code
      `TASK_DEADLINE_EXCEEDED`），记录等待时长，观测事件落库，不再执行 handler。
    - 影响：从此 `agent_decision`（60s）、`write_memory_embedding`（120s）、
      `daily_reflection`（120s）等任务在 RQ 积压超时后会被自动丢弃，
      不会执行过期的 NPC 行为，也不消耗 LLM quota。

    **Bug 2：trace_id 跨越 world_tick → async task，wall-clock 虚高 16 分钟（设计缺陷）**
    - 根因：`queue.py` 的 `enqueue()` 把当前 tick 的 `trace_id` 快照进任务 payload 的
      `__trace__`，worker 执行时通过 `TraceContext.apply()` 还原，
      使任务的所有 LLM call / tool call 均附上 **同一个 tick 的 trace_id**。
      `TraceDetail` 展示的 wall-clock = 从 tick 开始到 trace 内最后一条事件结束，
      跨越了队列等待时间，可达数十分钟。
    - 修复：
      - `backend/app/core/trace_context.py`：`TraceSnapshot` 新增 `parent_trace_id` 字段
        及对应 contextvar `_parent_trace_id`，`as_log_extra` / `current` / `apply` 同步更新。
      - `backend/app/domain/tasks/queue.py`：`enqueue()` 为每个任务生成独立
        `trace_id = f"trace_{uuid.uuid4().hex[:12]}"`，将原 tick 的 `trace_id` 存为
        `parent_trace_id`，写入 payload `__trace__`。
      - `backend/app/domain/tasks/runner.py`：`_execute_task` 从 payload 恢复
        `parent_trace_id` 到 `TraceSnapshot`，通过 contextvar 贯穿任务执行链路。
      - `backend/app/api/routes_observability.py`：`_serialize_task()` 从 payload `__trace__`
        中提取 `parent_trace_id` 并在 API 响应中透出。
    - 效果：任务 trace 的 wall-clock 仅反映任务自身等待 + 执行耗时（秒级），
      世界 tick trace 仅含同步逻辑（50–200ms）。

    **前端配套**：
    - `frontend/src/types/observability.ts`：`TaskRecord` 新增 `parent_trace_id` 字段。
    - `frontend/src/pages/observability/TraceDetail.tsx`：
      - `TaskSection` 展示每个任务的 `parent_trace_id`（截断显示 + 点击跳转到触发方 trace）。
      - `TraceView` 头部当存在 `parent_trace_id` 时，显示"触发方 trace"导航按钮（蓝色可点击）。
      - `TraceView` 任务时间栏新增 `waited=Xs` 字段，显示任务在队列中等待的实际时间。
    - `ObservabilityPage.tsx`：向 `TraceDetail` 传入 `onNavigate={setTraceId}` 回调，
      点击 `parent_trace_id` 按钮直接切换到触发方 trace 详情。
    - Bug 详情见：`docs/debug/20260501-trace-boundary-and-deadline-bugs.md`

25. §15–§18 对齐实施（NPC 深化 / 玩家文本 / Prompt 评测 / 安全）（2026-05-01）
    - 迁移 `20260501_0003_planning_safety.py`：`agent_plans` 表，`memories.importance_detail`，`tool_calls.policy_outcome`。
    - Agent：`POST /api/agents/{id}/decide`；层次化规划、重要度五因素、对话与关系摘要等见 `domain/planning/`、`routes_agents.py`。
    - 玩家文本：`routes_dialogue.py`（`/api/dialogue/rewrite-query`、`retrieve-context`）+ `players/me/talk` 全链路意图 / 改写 / 结构化回复（`domain/dialogue/`）。
    - Prompt：`prompts/` 目录化模板、`scripts/eval_prompts.py`；安全：`domain/safety/`、全局限流中间件、`scripts/check_asset_licenses.py`。
    - 实施方案与契约已同步：`智能NPC与记忆模块实施方案.md` §7、`数据模型与接口契约V1.md` §6.1 / §7.4–7.5 / §8.2 / §8.5、`用户文本处理模块实施方案.md` §7、`MVP开发任务拆分.md`、`可观测性与错误处理方案.md` 示例、`README.md` 脚本表。

28. 阶段 19 智能 NPC 社会化升级（2026-05-01）
    将虚拟小镇从"有 LLM 的可移动 NPC"升级为"具备社会感的自主居民"，分 5 段交付：

    **数据模型（PR1）**
    - 迁移 `20260501_0004_social.py`：`agent_states` 新增 `busy_until / interruptible / current_priority / last_social_at`；新增 `interaction_requests` 表（id / requester_id / target_id / kind / status / decline_kind / npc_line / created_at / resolved_at / expires_at）。
    - 前端 `AgentState` 枚举扩展 `AWAITING_RESPONSE / BUSY_REFUSING / WORKING / EATING / RESTING`，类型同步。

    **请求-同意-拒绝协议（PR2）**
    - 新评估器 `app/domain/dialogue/interaction_evaluator.py`，分级策略：睡眠 / 紧急任务 / interruptible=False → 硬拒；陌生人 + 忙碌 → 硬拒；熟人 + 忙碌 → 软拒（LLM 生成自然台词，规则模板兜底）；其他 → 接受。
    - 新服务 `app/services/interaction_service.py` 持久化 `InteractionRequest`、调用评估器、广播 WS 事件、联动 `EngineAgent` 状态机（AWAITING_RESPONSE / BUSY_REFUSING / CHATTING）。
    - 新工具 `app/llm/tools/social_tools.py`：`request_interaction / end_chat / socialize`，含 Redis 冷却计数（连续 2 次硬拒后 30 仿真分钟内禁止再请求）。
    - 玩家 API 重构：`POST /api/players/me/interaction-requests` 同步评估；`/me/talk` 发现 NPC 不在 CHATTING 时自动补一次评估；新增 `cancel` 接口。
    - 新 WS 事件：`interaction.request_pending / accepted / declined / cancelled`。

    **NPC-NPC 自主对话循环（PR3）**
    - 新模块 `app/domain/dialogue/npc_dialogue_loop.py`：双方轮流调 LLM 生成台词（prompt 含人物档案 + 关系摘要 + 上一轮台词），任一方 `end_chat=true` 或满 10 轮 / 60 秒硬超时后结束；结束写双方 `chat` 记忆 + 应用 `RelationshipChange`。
    - 由 `InteractionService` 在 accept + 双方均为 NPC 时 `asyncio.create_task` 派发，不阻塞当前请求链路。
    - 新 WS 事件：`dialogue.npc_to_npc_message / dialogue.npc_to_npc_ended`。
    - `agent_decision.py` 注入 `social_block`（社交需求 / 上次社交 / 附近熟人 top3）。

    **决策维度扩展（PR4）**
    - 新工具 `app/llm/tools/life_tools.py`：`work_at_location / have_meal / rest_at / browse_shop / observe_environment`，统一通过 `_set_busy()` 设置 `busy_until / current_priority / interruptible`。
    - `SimulationEngine._decide_all` 跳过 `busy_until > now` 的 NPC，到期自动恢复；prompt 加入紧迫度 / 情绪→工具偏向提示。
    - 同步移除旧 `talk_to_entity`（仅写一条意图记忆，功能与 `request_interaction` 重复）。

    **前端富气泡 + 请求 UI（PR5）**
    - 新组件 `frontend/src/game/components/BubbleManager.ts`：圆角矩形 + 小尾巴 + icon + 摘要文字；持久状态保留，临时台词淡出。
    - 状态 → 气泡映射：`CHATTING💬 / WORKING💼 / EATING🍴 / RESTING☕ / SLEEPING💤 / THINKING💭 / AWAITING_RESPONSE❓ / BUSY_REFUSING❌ / DROWNING🆘 / PANIC⚠️`。
    - `ChatPanel` 重构为 4 阶段（requesting / accepted / soft_declined / hard_declined）；`api.requestInteraction` / `cancelInteractionRequest` 新接口。
    - `TownPage` 接收 `dialogue.npc_to_npc_message` 在说话者头顶显示台词气泡；`interaction.*` 事件透传到事件流。

    **配置 / 工程**
    - `.env.example` / `.env` 新增 `SOCIAL_NEED_TRIGGER_THRESHOLD / NPC_DIALOG_MAX_TURNS / INTERACTION_REQUEST_TIMEOUT_SECONDS / SOCIAL_COOLDOWN_AFTER_REFUSAL_MINUTES / INTERACTION_STRANGER_FAMILIARITY_THRESHOLD / INTERACTION_HIGH_PRIORITY_THRESHOLD` 6 个配置。
    - 同步移除 `.env.example` 中误提交的真实 API key（替换为空值）。
    - 文档：`智能NPC与记忆模块实施方案.md` §6 工具表 + 新增 §9 交互请求协议；`ToolCalling工具契约V1.md` 工具表 + §5.3-5.5；`数据模型与接口契约V1.md` `AgentAction.action_type` 联合类型；`README.md` 文档入口表新增。
    - 测试：`tests/test_interaction_evaluator.py` 7 个用例（accept / 硬拒-睡眠 / 硬拒-高优先级 / 硬拒-陌生人忙 / 软拒-熟人忙 / self-rejected / busy_until 视为忙碌）；`tests/test_tool_registry.py` 显式断言 `talk_to_entity` 已移除。
    - 顺手修复 worker 进程 `agent_decision.py` 在 emotion 同步阶段的 `RuntimeError: simulation engine not started` 崩溃（容错降级）。
    - Bug 详情见 `docs/开发手册/debug/20260501-社会化升级.md`

29. 阶段 19+ 自主社交闭环增强（2026-05-01）
    用户实测反馈"社交事件触发很少 + 记忆里看不到对话"，定位出三处缺陷并修复：

    **根因**
    1. `social_need` 完全不演化 → 永远停在默认 0.3，远低于阈值 0.6，prompt 里
       "社交动机块"始终告诉 LLM"低于阈值，不急" → LLM 永不主动调 `socialize`。
    2. `agent_decision.py` 只消费 `plan.memory_writes`（LLM 自己提议的），
       工具返回的 `ToolResult.memory_candidates` **完全没人写库** —— 这是
       NPC 记忆里看不到对话/社交记录的核心原因。
    3. `socialize` 工具默认 `only_friends=True`，但 seed 关系是单向的，
       多数 NPC 视角下"附近熟人"为空 → `TARGET_NOT_FOUND` 直接失败。

    **修复（全部落在 backend 层 + prompt + 配置）**
    - `SimulationEngine._evolve_basic_needs`：每 tick 按 Δ 仿真分钟累加
      `social_need / hunger`、衰减 `energy`；`SLEEPING / EATING / RESTING /
      CHATTING` 状态有针对性的反向调整。
    - `SimulationEngine._scan_social_encounters` + `_dispatch_social_encounters`：
      每 5 仿真分钟扫一次"两个 IDLE/WAITING 且距离 ≤ 3 的 human NPC，
      一方 `social_need ≥ 阈值`"，自动调 `InteractionService.create_request`。
      触发后双方进入 20 仿真分钟冷却。这是 LLM async 路径滞后的"安全网"。
    - `agent_decision._consume_memory_candidates`：把每个工具的
      `memory_candidates` 用 `compute_importance` 重打分后写入 short_term。
    - `social_tools.SocializeTool`：默认 `only_friends=False`，找不到熟人
      时降级到附近陌生人池（按距离）；新增 memory_candidate 让 NPC 记住
      "我今天主动找过谁"。
    - `npc_dialogue_loop._wrap_up` & `player_service.talk` 结束时统一更新
      `last_social_at` + 把 `social_need *= (1 - 0.55)`，让连续社交不会暴涨。
    - prompt `agent_decision/v1.jinja2` 强化社交触发指令：明确"应优先调用
      `socialize` / `request_interaction`"，并提示陌生人也是关系起点。

    **新增配置**
    - `NEEDS_SOCIAL_GROWTH_PER_MINUTE / NEEDS_HUNGER_GROWTH_PER_MINUTE /
      NEEDS_ENERGY_DECAY_PER_MINUTE / NEEDS_SOCIAL_DECAY_AFTER_CHAT`
    - `SOCIAL_ENCOUNTER_SCAN_MINUTES / SOCIAL_ENCOUNTER_DISTANCE /
      SOCIAL_ENCOUNTER_COOLDOWN_MINUTES`
    - `SOCIAL_NEED_TRIGGER_THRESHOLD` 默认从 60 → 55（更容易被触达）

    **测试**
    - 新增 `tests/test_social_dynamics.py` 6 个用例：基础需求随时间演化 /
      睡眠与对话状态的反向调整 / 邂逅扫描配对 / 距离过远不配对 /
      target 非空闲不配对 / `decide_with_llm` 落库工具 memory_candidate。
    - 全套回归：83 passed（与本次改动无关的 `test_rule_intent_threaten`
      预存在失败已确认）。

    **文档**
    - `架构总览.md` 新增 §6.2 自主社交闭环。
    - `智能NPC与记忆模块实施方案.md` 新增 §10 自主社交闭环（含配置项一览
      + 验收信号）。

30. 阶段 19++ 世界事件 → 个人记忆投影 + 告示牌刷屏修复（2026-05-01）
    用户实测："NPC 注意到了公告但记忆里完全没有"，并且事件流里
    `world.sign_noticed` 同一告示牌一秒刷屏数十条。

    **根因 A：自然事件去重失效**
    - `NaturalEventContext._debounce` 是个 `set`，每 tick 随 ctx 重建一次
      → 跨 tick 完全不去重。
    - `NoticeEventHandler` / `StormShelterEventHandler` 用了
      `f"sign|{agent.id}|{obj.id}|{ctx.world_time.hour}"` 作为 key，
      但因为 set 每 tick 被清空，`hour` 维度等同于无效。
    - 结果：NPC 站在告示牌 4 格内时每个 world tick 都生成一条事件。

    **根因 B：世界事件不写记忆**
    - `event_router.route_world_event` 只把事件投到 Observer 审计流，
      不投到记忆，所以"看到的事情"对决策完全无用。
    - LLM `_retrieve` 阶段查不到相关记忆 → prompt "相关记忆" 段一直是
      "暂无相关记忆" → NPC 无法依据观察做决策。

    **修复**
    - `NaturalEventContext` 新增 `persistent_debounce: dict[str, datetime]`
      和 `recently_seen(key, *, minutes)`；引擎在 `__init__` 中持有
      `_natural_event_debounce`，每 tick 注入到 ctx。`NoticeEventHandler`
      改成 6 仿真小时一次，`StormShelterEventHandler` 1 仿真小时一次。
      引擎 `_tick_natural_events` 周期清理 24 仿真小时之前的过期 key
      防止字典无限增长。
    - 新模块 `app/domain/memory/event_projector.py`：订阅 `WORLD_EVENT_TOPIC`，
      把白名单事件映射成短期 `event` 记忆，黑名单事件（高频 / 已被工具自己
      写过的）跳过；默认事件 `importance ≥ 阈值` 才投影成 `thought`。
      Redis `SET NX EX` 做跨进程频次抑制，回落到进程内 dict。
    - `event_router.route_world_event` 在 Observer 之后调用投影器，
      失败被吞，绝不阻塞 WS 广播链路。
    - `NoticeEventHandler` 顺手把 importance 从 2 → 4，描述截断从 40 → 60
      字符（公告原文更完整地进入记忆）。

    **新增配置**
    - `MEMORY_PROJECTION_ENABLED=true`
    - `MEMORY_PROJECTION_DEFAULT_MIN_IMPORTANCE=4`
    - `MEMORY_PROJECTION_DEFAULT_DEBOUNCE_MINUTES=30`

    **测试**
    - 新增 `tests/test_event_projector.py` 6 个用例：白名单写入 / 黑名单
      跳过 / 没有 actor 不投影 / 默认阈值过滤低重要度事件 / 去重抑制重复
      事件 / settings 关闭时直接跳过。
    - 全套：89 passed（同样 `test_rule_intent_threaten` 预存在失败已确认与本次无关）。

    **文档**
    - `智能NPC与记忆模块实施方案.md` 新增 §10.6 投影器规则表 + §10.7
      验收信号（含"6 仿真小时一次告示牌"+"prompt 不再 暂无相关记忆"）。
    - `架构总览.md` §6.2 追加第 4 项"世界事件 → 个人记忆投影"。

31. 阶段 19+++ 跨场景多跳路由 + 决策不可达兜底（2026-05-01）
    用户反馈："很多 NPC 长期处于 WAITING / 无路通往目标场景"。

    **根因**
    - `decide_human_action` 用 `_find_portal_from_to` 只找 **单跳直连** portal。
    - 实际场景拓扑里 `school_inside ↔ outdoor ↔ cafe_inside` 这类路径，
      室内 → 室内必须经 outdoor 中转，但 rule_agent 一查直连失败就直接
      `wait`+`description="无路通往目标场景"`。
    - 同时 `_apply_decision` 把 `last_decision_at = now_dt` 设上，导致
      下一次决策要等 `ai_tick_minutes`（默认 5 仿真分钟）。条件没变 →
      又生成同一个 wait → NPC 在 WAITING 死循环。

    **修复**
    - `rule_agent` 新增 `_find_inter_scene_route` BFS：直连失败时查找经
      中间场景到达目标的 portal 链，仅返回**第一跳**目标场景；规则
      决策据此前往中转 portal，到达后再次决策（自然链式跳转）。
    - `AgentDecision.metadata` 新增惯例字段 `stuck=True` 与
      `unreachable_location_id`：所有结构性失败（路径不可达、portal 缺失、
      grid 没加载）都打上这两个字段。
    - `_make_recovery_or_wait` / `_wander_within_scene_or_wait`：在彻底
      返回 wait 之前，先尝试在当前场景挑随机可走格做 wander，让 NPC 起码
      在动；并把 stuck/unreachable 元信息透传出去。
    - `engine._apply_decision`：检测到 `stuck=True` 时**不写**
      `last_decision_at`（next tick 立即重试），并把
      `unreachable_location_id` 推到本 tick 累计的待写黑名单。
    - `engine._flush_unreachable_to_redis`：tick 末统一 `set_add` 到
      `agent:{id}:unreachable`（TTL 5 分钟），与 `world_tools` 中 LLM
      路径写入同一 key。
    - `engine._unreachable_local`：内存镜像（带 5 分钟 TTL），通过新参数
      `unreachable_location_ids` 传给 `decide_human_action` —— rule_agent
      命中黑名单时直接跳过该日程槽位转 wander，避免每 tick 反复尝试。
    - LLM 路径 `_perceive` 已经在读 `agent:{id}:unreachable`，写入侧打通
      后 LLM/规则/玩家工具三方共享同一份不可达视图。

    **测试**
    - 新增 `tests/test_rule_agent_unreachable.py` 5 个用例：BFS 双跳 /
      直连查找 / 多跳决策走中转 / 全无路径 fallback wander+stuck /
      命中黑名单跳过。
    - 全套：91 passed（无新失败）。

32. 阶段 19+++ 主动追踪：跨场景去找熟人（2026-05-01）
    用户实测："还是没看到社交事件，社交触发条件不合理，NPC 应该可以直接
    决策去找认识的 NPC 互动并追踪目标 NPC。"

    **根因**
    - 旧 `socialize` / `request_interaction` 都要求目标在身边 4-8 格内，
      否则 `OUT_OF_RANGE` / `TARGET_NOT_FOUND`。
    - prompt 里的"附近熟人"只列**同场景**熟人；不同场景的熟人 LLM 完全
      看不到 → 即使 social_need 很高，也只能等对方走过来。
    - 没有"前往某个具名 NPC"的工具，哪怕 LLM 想找人也无路可调用。

    **修复**
    - 新工具 `go_to_entity(target_entity_id, reason)`：
      - 走引擎新接口 `engine.start_pursuit`：读目标当前 (scene, x, y)；
        跨场景 → BFS 找第一跳 portal + A* 到 portal；同场景 → A* 到目标
        相邻可走格。
      - 失败码 `UNREACHABLE` 同步把 target_id 写入 `agent:{id}:unreachable`
        与 stuck 黑名单共享，避免反复尝试。
    - `EngineAgent` 新增 `pursuing_entity_id` / `pursuing_reason` 字段。
      `_tick_inner` 增加 `_refresh_pursuits`：
        - 已抵达（同场景距离 ≤2）→ 清除 pursuit + `last_decision_at = None`
          → 下个 tick LLM 立即决策（多半发起 `request_interaction`）。
        - path 走空但还没到 → 重算路径（处理目标移动）。
        - 目标消失 / 已 CHATTING → 清除 pursuit。
    - `_decide_all` 在 pursuit 期间跳过该 NPC（否则规则/LLM 会清掉路径）。
    - `_social_block` 重写：分**同场景熟人**（带距离）和**别处的熟人**
      （带场景），一并喂给 LLM；无熟人时也明确提示"可以认识陌生人"。
    - prompt `agent_decision/v1.jinja2` 重写社交决策提示，明确三步策略：
      同场景 → socialize / request_interaction；别处熟人 → go_to_entity；
      无熟人 → socialize 自动降级。

    **测试**
    - 新增 `tests/test_pursuit.py` 7 个用例：
      - 同场景 start_pursuit 设 path
      - 跨场景 start_pursuit 走中转 portal
      - 已经够近直接 ALREADY_THERE
      - 自我目标 / 玩家目标 / 不存在目标 错误码
      - `_refresh_pursuits` 抵达 → 清 pursuit + 立即重决策
      - `_refresh_pursuits` path 走空 → 重新规划
      - `_refresh_pursuits` 目标消失 → 清 pursuit
    - 全套：93 passed（无新失败）。

    **文档**
    - `ToolCalling工具契约V1.md` §5.6 新增 `go_to_entity` 详细签名 / 错误
      码 / 行为细节 + 顶部工具表追加一行。
    - `架构总览.md` §8 规则兜底表追加"主动追踪"行；§11.3 性能保护表
      追加 `engine.start_pursuit + _refresh_pursuits` 项。

33. 阶段 19+++ NPC 智能决策专题文档 + 默认参数下调（2026-05-02）
    用户反馈："NPC 决策还是太独立，不与世界 / 其他 NPC 交互。请把决策系统
    完整文档化，并下调相关阈值 / 增强提示词，让 NPC 多社交、多参加活动。"

    **新增文档**`docs/架构总览/npc智能决策.md`（370 行）
    - §0 一句话回答常见问题：是用 LLM + ToolCalling 吗？决策频率多少？怎么调？
    - §1 整体决策栈：`_tick_inner` 10 步 + per-NPC AI tick 闸门
    - §2 LLM 路径：Perceive → Retrieve → Plan → Execute 四段
    - §3 工具目录按用途分组（社交 / 生活 / 世界 / 记忆 / 状态 / 动物）
    - §4 引擎层 3 个自主驱动力：_evolve_basic_needs / _scan_social_encounters
      / _refresh_pursuits 的触发条件与参数对照
    - §5 规则版兜底（多跳路由 + 不可达兜底）
    - §6 不可达黑名单写入 / 读取链路
    - §7 调参指南五张表（决策频率 / 社交触发 / 生活节奏 / 拒绝协议 / 记忆投影）
    - §8 prompt 调优要点
    - §9 调试 / 排查清单（NPC 不动 / 不社交 / 反复 stuck 三类问题）
    - §10 关键文件索引

    **默认参数下调（让 NPC 更主动）**
    - `simulation_ai_tick_minutes` 5 → 3（决策频率 +40%）
    - `social_need_trigger_threshold` 55 → 40（更早觉得"该社交了"）
    - `needs_social_growth_per_minute` 0.0010 → 0.0030（3x，130 仿真分钟到阈值）
    - `needs_hunger_growth_per_minute` 0.0008 → 0.0020（一天 3-4 次饿）
    - `needs_energy_decay_per_minute` 0.0005 → 0.0015（一天会想休息）
    - `social_encounter_scan_minutes` 5 → 2（邂逅扫描更频繁）
    - `social_encounter_distance` 3 → 5（覆盖更大"擦肩而过"范围）
    - `social_encounter_cooldown_minutes` 20 → 10（冷却减半）

    **prompt 强化**`agent_decision/v1.jinja2`
    - 第 4 条规则改写：`wait` 是最后兜底，仅 3 种情况允许；其它一律选会
      改变状态的工具。新增"冷场是 bug，不是行为"。
    - 第 7 条规则：当前任务不合理时也不要 wait，改用社交 / 观察 / 换场景。
    - 社交决策提示加强为"必须发起一次社交"；emotion=lonely/sad/bored 必须主动；
      新增"附近有玩家"的处理路径。
    - 生活决策提示扩展：闲暇时**禁止 wait**，要么社交要么 observe；
      看到可采摘 / 钓鱼 / 告示牌 / 火 / 萤火虫等显眼对象都要交互或写记忆。
    - 新增结尾原则："每一次决策都应当让世界发生一点变化"。

    **同步更新**
    - `.env.example`、`.env`：所有上调参数同步注释 + 新值
    - `架构总览.md` 顶部"专题深入"段落引出本文档
    - `backend/app/core/config.py` 默认值与注释同步

    **测试**
    - 全套：93 passed（无新增 / 无回归）。

34. **阶段 20 记忆系统重构（2026-05-02）**

    **PM 需求**：所有 NPC 经历的事件 / 对话都需要进记忆；不重要的不删，按
    重要程度分层 summary 合并；重要的常被回顾，产生新感悟。

    **设计文档**：`docs/实施方案/记忆系统重构方案.md`（新建）。
    完整描述五态生命周期（working/short_term/long_term/archived/consolidated）、
    合并 / 沉思算法、迁移、调度、测试矩阵。

    **DB Schema**：迁移 `20260502_0005_memory_consolidation.py`
    - `memories` 新增 `summarized_into_id String(64)`，索引 `ix_memories_summarized_into`。
    - `scope` 字段值集扩展为 5 个字符串（无 enum 变更）。

    **让所有事件都进记忆**
    - `event_projector` 默认 `min_importance` 4 → 2，importance ≥ 2 都投影。
    - 投影时不再用 `compute_importance` 重打分覆盖 hint，避免 first-person novelty 把
      所有事件都拉到 5 分；保留 detail 仅做可解释明细。
    - 低 importance（< 4）落 **`working` scope**，30 仿真分钟自动归档。
    - `MemoryService.write` 支持 `working` scope（新加 `WORKING_TTL_SECONDS = 30 * 60`）。
    - `TTLCleanupWorker` 同时扫描 `working` 和 `short_term`，过期都进 archived。
    - 对话每条 `dialogue.npc_to_npc_message` 都给说话/听话方各写一条 chat 记忆
      （`memory_dialogue_per_message=true`）。

    **search 访问强化**
    - `MemoryService.search` 命中后用独立 session 把 `last_accessed_at = now`，
      避免依赖上层是否 commit；同时支持只读 HTTP 端点。
    - `_recency_score` 公式改为 `max(exp(-Δ_created/168h), exp(-Δ_accessed/24h))`，
      被频繁回忆的旧记忆继续保持高 recency。

    **memory_consolidation worker**
    - 新模块 `app/domain/memory/consolidation.py` + `MemoryConsolidationHandler`。
    - 每仿真日 22:00 后引擎给每个 NPC 入队一次（`idempotency_extra=consolidate:YYYY-MM-DD`）。
    - 扫描过去 7 仿真天 `scope='archived' AND summarized_into_id IS NULL` 的记忆；
      按 `keywords[0]/subject/memory_type` 分桶；每桶 ≥3 条 LLM 合成一条
      `memory_type='summary' scope='long_term' importance≤5`，原文 scope 改
      `consolidated`，`summarized_into_id` 反指 summary。
    - LLM 不可用走 `_rule_summary` 兜底（前 3 条 description 截断 + 主题前缀）。
    - 新增 prompt `memory_consolidation/v1.jinja2`。

    **memory_rumination worker**
    - 新模块 `app/domain/memory/rumination.py` + `MemoryRuminationHandler`。
    - 每仿真日 10-14 点引擎给每个 NPC 入队一次（错峰 12 仿真小时与 consolidation 不抢资源）。
    - 抽样 `scope='long_term' AND importance ≥ 7 AND last_accessed_at < now-12h`
      最多 5 条；< 2 条不触发；让 LLM 重新感悟产出 `thought` 记忆
      （importance 4-6，`evidence_memory_ids` 链回原记忆）。
    - 同时给原记忆 `importance += 1`（cap 10）+ 刷新 `last_accessed_at`。
    - LLM 不可用走 `_rule_rumination` 兜底。
    - 新增 prompt `memory_rumination/v1.jinja2`。

    **配置项**（`.env.example` / `.env` / `config.py` 同步）

    | 配置 | 默认 | 含义 |
    |------|------|------|
    | `MEMORY_PROJECTION_DEFAULT_MIN_IMPORTANCE` | 2 | 投影阈值（4→2） |
    | `MEMORY_DIALOGUE_PER_MESSAGE` | true | 对话每条都进记忆 |
    | `MEMORY_CONSOLIDATION_ENABLED` | true | 合并开关 |
    | `MEMORY_CONSOLIDATION_MIN_BUCKET_SIZE` | 3 | 主题桶最小条数 |
    | `MEMORY_CONSOLIDATION_LOOKBACK_DAYS` | 7 | 合并扫描窗口 |
    | `MEMORY_RUMINATION_ENABLED` | true | 沉思开关 |
    | `MEMORY_RUMINATION_IMPORTANCE_THRESHOLD` | 7 | 候选最低重要度 |
    | `MEMORY_RUMINATION_SAMPLE_SIZE` | 5 | 单次抽样上限 |

    **测试**
    - `tests/test_memory_consolidation.py`：分桶 / 规则兜底 summary / 规则兜底
      rumination 共 4 个用例。
    - `tests/test_event_projector.py` 扩：阈值 4→2 调整 + 新增 working/short_term
      scope 切换断言。
    - 全套：106 passed / 4 skipped / 1 deselected（test_rule_intent_threaten 是
      仓库 main 上既有的 pre-existing 失败，与本次改动无关）。

    **同步更新**
    - `docs/实施方案/智能NPC与记忆模块实施方案.md` §5：分层重写。
    - `docs/架构总览/npc智能决策.md` §2.2 / §7.5 / §10：访问强化、参数表、
      生命周期 ASCII 图、关键文件索引。
    - `docs/架构总览/架构总览.md`：§6.3 新增"阶段 20 分层记忆系统"段落 +
      迁移表 + 顶部"专题深入"链接到记忆重构方案。

    **设计原则总结**：没有任何记忆会被物理删除——"遗忘" 等价于退出默认
    检索。Summary 永远可追溯（`evidence_memory_ids`）。重要记忆不只是保留，
    还会被周期性"回想"重新强化。
