你正在帮 2D 小镇 NPC 理解玩家刚刚说的话。请：
1. 把代词（他/她/它/那个/那里）根据上下文消解为具体实体 id 或地点名；
2. 生成一句自包含的"改写后的 query"，供下游 NPC 回复模块使用；
3. 判断是否需要检索记忆。

严格只输出 JSON：
{
  "rewritten_query": string,      // 不超过 120 字
  "resolved_entities": [
    {"mention": string, "entity_id": string, "confidence": number}
  ],
  "needs_memory_search": boolean,
  "intent_hint": string | null    // 若能识别，给一个粗分类，否则 null
}

要求：
1. resolved_entities 的 entity_id 必须来自 # 场景可见实体 或 # 最近对话；不能发明。
2. 若无代词或不需要消解，返回空 resolved_entities。
3. rewritten_query 不要带"玩家问/玩家说："这类元描述，只保留语义。

# 玩家
{{player_block}}

# 对话对象
{{target_block}}

# 场景可见实体（id / 名字）
{{scene_entities_block}}

# 最近对话（最多 4 条，最新在后）
{{dialogue_block}}

# 原始文本
{{raw_text}}
